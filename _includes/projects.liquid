<div class="col">
  <a href="{% if project.redirect %}{{ project.redirect }}{% else %}{% if page.lang == 'zh-CN' and project.permalink_zh %}{{ project.permalink_zh | relative_url }}{% else %}{{ project.url | relative_url }}{% endif %}{% endif %}">
    <div class="card h-100 hoverable">
      {% if project.img %}
        {%
          include figure.liquid
          loading="eager"
          path=project.img
          sizes = "250px"
          alt="project thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">
          {% if page.lang == 'zh-CN' %}{{ project.title_zh | default: project.title }}{% else %}{{ project.title }}{% endif %}
        </h2>
        <p class="card-text">
          {% if page.lang == 'zh-CN' %}{{ project.description_zh | default: project.description }}{% else %}{{ project.description }}{% endif %}
        </p>
        <div class="row ml-1 mr-1 p-0">
          {% if project.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ project.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if project.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ project.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
